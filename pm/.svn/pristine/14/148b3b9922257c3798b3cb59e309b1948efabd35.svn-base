package com.cninsure.payment.util.biz;

import java.io.IOException;
import java.net.URL;

import org.springframework.core.io.Resource;
import org.springframework.core.io.support.PathMatchingResourcePatternResolver;
import org.springframework.core.io.support.ResourcePatternResolver;

public class BizCore {

	public static String HandlerSignmsg(String unSignStr) {

		ResourcePatternResolver resolver = new PathMatchingResourcePatternResolver();
		// 只加载一个绝对匹配Resource，且通过ResourceLoader.getResource进行加载
		Resource[] resources = null;
		try {
			resources = resolver.getResources("classpath:common/88888888.pfx");
		} catch (IOException e1) {
			e1.printStackTrace();
		}
		String pathStr = "";
		try {
			URL path = resources[0].getURL();
			pathStr = path.getPath().substring(1);
		} catch (IOException e1) {
			e1.printStackTrace();
		}

		String handlerStr = unSignStr.substring(0, unSignStr.length() - 9);
//		String signStr = UtilMD5.crypt(handlerStr);
//		try {
//			return UtilSHA1.fileSignMessage(signStr.toLowerCase(), pathStr,
//					"12345678");
//		} catch (Exception e) {
//			e.printStackTrace();
//		}
		return "";
	}

	public static void main(String[] args) {
	}
}
